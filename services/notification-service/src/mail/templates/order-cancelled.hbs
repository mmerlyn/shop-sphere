<!DOCTYPE html>
<html>
<head><meta charset="utf-8"></head>
<body>
  <h1>Order Cancelled</h1>
  <p>Hi {{firstName}},</p>
  <p>Your order #{{orderNumber}} has been cancelled.</p>
  {{#if reason}}
  <p>Reason: {{reason}}</p>
  {{/if}}
  {{#if refundAmount}}
  <p>Refund Amount: ${{refundAmount}}</p>
  {{/if}}
  <p>Visit <a href="{{shopUrl}}">ShopSphere</a> to place a new order.</p>
</body>
</html>
